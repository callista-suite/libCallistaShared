#pragma once

#include <callista/types.hpp>

// Documents are the base type for all open files,
// like each window with an image open in an image editor.
// Each application will have to subclass Document to fill out their needs.
class Document {
	
};